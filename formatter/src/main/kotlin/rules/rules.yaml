# Ejemplo de archivo de configuración del formatter en YAML
spaceBeforeColon: true # antes de :
spaceAfterColon: true # después de :
spaceAroundEquals: true # alrededor de =
lineFeed: 1 # salto de línea /n